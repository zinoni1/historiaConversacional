import java.util.ArrayList;

public class Bond extends Personatges {
    ArrayList<Objecte> Inventari = new ArrayList<Objecte>();

    public Bond(String nom, String descripcio) {
        super(nom, descripcio);
    }

    // crear funcio que retorni tots els personatges de la zona actual
    /*
     * public static void getArrayPersonatges() {
     * for (int i = 0; i < zonaActual.arrayPersonatges.size(); i++) {
     * System.out.println(zonaActual.arrayPersonatges.get(i).nom + ": " +
     * zonaActual.arrayPersonatges.get(i).descripcio);
     * }
     * }
     * 
     * //crear funcio que retorni tots els personatges de la zona actual
     * public static void getArrayZones() {
     * for (int i = 0; i < zonaActual.arrayZones.size(); i++) {
     * System.out.println(zonaActual.arrayZones.get(i).nom + ": " +
     * zonaActual.arrayZones.get(i).descripcio);
     * }
     * }
     * 
     * //crear funcio que retorni tots els personatges de la zona actual
     * public static void getArrayZonesAccesibles() {
     * for (int i = 0; i < zonaActual.arrayZonesAccesibles.size(); i++) {
     * System.out.println(zonaActual.arrayZonesAccesibles.get(i).nom + ": " +
     * zonaActual.arrayZonesAccesibles.get(i).descripcio);
     * }
     * }
     * 
     * //crear funcio que retorni tots els personatges de la zona actual
     * public static void getArrayZonesInaccesibles() {
     * for (int i = 0; i < zonaActual.arrayZonesInaccesibles.size(); i++) {
     * System.out.println(zonaActual.arrayZonesInaccesibles.get(i).nom + ": " +
     * zonaActual.arrayZonesInaccesibles.get(i).descripcio);
     * }
     * }
     * 
     * //crear funcio que retorni tots els personatges de la zona actual
     * public static void getArrayZonesInaccesiblesAmbTargeta() {
     * for (int i = 0; i < zonaActual.arrayZonesInaccesiblesAmbTargeta.size();
     */
}
